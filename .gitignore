# --- Node / Electron / Next.js ---
node_modules/
.next/
out/
dist/
dist-electron/
build/
release/
next-env.d.ts
*.tsbuildinfo
.env
.env.local
.env.*.local
npm-debug.log*
yarn-error.log*
.eslintcache

# --- Gradle / Java ---
.gradle/
mod/build/
mod/run/
mod/.gradle/
mod/.gradle-dist/
*.class
*.log
hs_err_pid*
replay_pid*

# --- IDE ---
.idea/
.vscode/
*.iml
*.ipr
*.iws
.settings/
.classpath
.project

# --- OS ---
.DS_Store
Thumbs.db
desktop.ini

# --- BestClient local runtime data ---
.bestclient/
launcher/electron-dist/

# Per-installation Azure client ID - see README
launcher/resources/auth.json

# Update token for a private release repo. Never commit this: anyone with the file can
# read the repository it is scoped to. Keep it fine-grained, read-only and single-repo.
launcher/resources/update-token.json
update-token.json

# The bundled mod jar is produced by `mod/gradlew build`, not tracked.
launcher/resources/mods/*.jar
